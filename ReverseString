using System;
					
public class Program
{
	public static void Main()
	{
		string str = "ABC";
		string reverse = "";
		for(int i = str.Length-1; i>=0 ; i--)
		{
			reverse += str[i];
		}
		Console.WriteLine("Reverse of ABC is " +reverse);
	}
}
